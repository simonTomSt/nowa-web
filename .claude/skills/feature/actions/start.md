# Start Action

1. Confirm the feature goals are clear (from "load" or inline description)
2. If no goals are defined, error: "Run /feature load first"
3. List the goals, then implement them one by one
